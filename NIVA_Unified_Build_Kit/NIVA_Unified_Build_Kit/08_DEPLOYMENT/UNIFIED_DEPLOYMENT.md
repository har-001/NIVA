# Unified Deployment
Web follows its existing deployment. Backend runs on selected server/cloud. Tauri produces signed desktop artifacts. Expo development builds and Android release use the appropriate Expo/Android workflow. Server secrets never enter client builds.
