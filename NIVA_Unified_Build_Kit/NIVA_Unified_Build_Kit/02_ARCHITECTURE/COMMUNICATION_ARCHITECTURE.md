# Communication
Intent -> contact/channel resolution -> auth -> policy -> provider/device adapter -> action -> verification -> audit.
Messaging, email, calls, transcription and incoming-call assistance are provider/platform dependent. Recording is never hidden and must be authorized and compliant.
