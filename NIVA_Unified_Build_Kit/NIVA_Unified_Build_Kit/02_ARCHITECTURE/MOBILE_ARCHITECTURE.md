# Expo Mobile
Stack: Expo, React Native, TypeScript, Expo Router where useful, secure storage, notifications, camera and development builds for native modules.
Prefer shared backend AI rather than duplicating heavy inference on mobile.
