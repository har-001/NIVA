# Tauri Desktop
Stack: Tauri, React, TypeScript, Rust; Python local service only when useful for CV/voice/automation.
Layers: UI -> command/state layer -> Tauri commands -> permission/policy -> local tools -> OS.
Use typed commands and allowlists. Confirm destructive actions.
