{
  "name": "niva-monorepo",
  "version": "1.0.0",
  "description": "NIVA — Neural Intelligent Virtual Assistant",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "desktop": "cd desktop && npm start",
    "dev:tauri": "cd desktop-tauri && npm run dev",
    "build:tauri": "cd desktop-tauri && npm run build",
    "test": "cd server && npm test",
    "test:e2e": "cd server && npx tsx src/test-e2e.ts",
    "test:brain": "cd server && npx tsx src/test-ai-brain.ts"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
