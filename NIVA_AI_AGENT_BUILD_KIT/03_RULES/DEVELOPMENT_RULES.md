# Development Rules

INSPECT → PLAN → IMPLEMENT → TEST → FIX → DOCUMENT → HANDOVER

One coherent task at a time. Prefer modular interfaces and typed boundaries. Use migrations for persistent schema changes. Do not reset data or discard uncommitted user work.
